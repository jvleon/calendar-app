const initialState = {
  remindersList: null
}

const Reminders = (state = initialState, { payload, type }) => {
  switch (type) {
    default:
      return {
        ...state
      }
  }
}

export default Reminders